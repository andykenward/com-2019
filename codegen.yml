overwrite: true
schema: ${SCHEMA_PATH}
documents: "./**/*.graphql"
generates:
  generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
config:
  namingConvention:
    enumValues: keep
